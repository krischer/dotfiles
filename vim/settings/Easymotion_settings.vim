" Easymotion specific settings.

" Change the leader key. The default <leader><leader> shortcut is already
" occupied by LustyJuggler and quite deep in my muscle memory.
let g:EasyMotion_leader_key = ';'
