" Lusty Juggler specific settings

let g:LustyJugglerDefaultMappings = 0 " Suppress default mapping.

" <leader>, will launch the lusty juggler, if , is the leader than ,, will
" launch it.
nmap <leader>, :LustyJuggler<CR>
